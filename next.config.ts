import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'pbs.twimg.com'], // Add other domains as needed
  },
};

export default nextConfig;
